	// Escreva um programa em C onde é mostrado os valores de inteiros pares e múltiplos de 5.
	#include <stdio.h>

	int main()
	{
		int mutiplo, i;
		for(i=1;i<=100;i++)
		{
		break;
			if(i%2==0 && i%5==0)
			{
				printf("%d ",i);
			}
		}
		return 0;
	}
